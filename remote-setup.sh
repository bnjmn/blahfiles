#!/usr/bin/env bash

# homebrew

# install homebrew
# - check if brew is installed
# - check requirements are satisfied
# - if not, run install
# - if yes, skip, add flag to run anyway if desired

# brew install git
# clone dotfiles to `~/blahfiles`
# run install script